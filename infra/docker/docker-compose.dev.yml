version: '3.8'
services:
  user-service:
    build:
      context: ../../ # Monorepo root
      dockerfile: services/users/user-service/Dockerfile
      network: host
      extra_hosts:
        - "repo.maven.apache.org:104.18.19.12"
        - "repo1.maven.org:104.18.19.12"
        - "registry.npmjs.org:104.16.2.34"
    container_name: user_service_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    depends_on:
      - postgres
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres_dev:5432/community_platform
      - SPRING_DATASOURCE_USERNAME=admin
      - SPRING_DATASOURCE_PASSWORD=secret
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update

  product-read:
    build:
      context: ../../ # Monorepo root
      dockerfile: services/catalog/product-read/Dockerfile # Specify the Dockerfile path
      network: host
      extra_hosts:
        - "repo.maven.apache.org:104.18.19.12"
        - "repo1.maven.org:104.18.19.12"
        - "registry.npmjs.org:104.16.2.34"
    container_name: product_read_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    depends_on:
      - postgres
    ports:
      - "8082:8082" # Corrected port mapping
    environment:
      - SERVER_PORT=8082 # Corrected internal port
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres_dev:5432/community_platform
      - SPRING_DATASOURCE_USERNAME=admin
      - SPRING_DATASOURCE_PASSWORD=secret
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
      - GRPC_SERVER_PORT=9091 # Expose gRPC port for internal communication

  product-write:
    build:
      context: ../../ # Monorepo root
      dockerfile: services/catalog/product-write/Dockerfile # Specify the Dockerfile path
      network: host
      extra_hosts:
        - "repo.maven.apache.org:104.18.19.12"
        - "repo1.maven.org:104.18.19.12"
        - "registry.npmjs.org:104.16.2.34"
    container_name: product_write_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4
    depends_on:
      - postgres
      - kafka # Add Kafka dependency
    ports:
      - "8081:8081" # Corrected port mapping
    environment:
      - SERVER_PORT=8081 # Corrected internal port
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres_dev:5432/community_platform
      - SPRING_DATASOURCE_USERNAME=admin
      - SPRING_DATASOURCE_PASSWORD=secret
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
      - KAFKA_BOOTSTRAP_SERVERS=kafka_dev:9092 # Correct Kafka address for Docker

  inventory-write:
    build:
      context: ../../ # Monorepo root
      dockerfile: services/inventory/inventory-write/Dockerfile
    container_name: inventory_write_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4
    depends_on:
      - postgres
      - kafka
    ports:
      - "8088:8080"
    environment:
      - DATABASE_URL=postgres://admin:secret@postgres_dev:5432/community_platform
      - KAFKA_BOOTSTRAP_SERVERS=kafka_dev:9092
      - KAFKA_GROUP_ID=inventory-write-group
      - PRODUCT_EVENTS_TOPIC=product-events
      - CHECKOUT_EVENTS_TOPIC=checkout.checkout-events

  cart-crud:
    build:
      context: ../../ # Monorepo root
      dockerfile: services/cart/cart-crud/Dockerfile # Specify the Dockerfile path
      network: host
      extra_hosts:
        - "repo.maven.apache.org:104.18.19.12"
        - "repo1.maven.org:104.18.19.12"
        - "registry.npmjs.org:104.16.2.34"
        - "node-precompiled-binaries.grpc.io:104.21.13.85"
    container_name: cart_crud_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    depends_on:
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
    ports:
      - "3001:3000" # Mapping to 3001 on host to avoid conflict with Grafana
      - "50050:50050" # Map gRPC port
    environment:
      - PORT=3000
      - DB_HOST=postgres_dev
      - DB_USER=admin
      - DB_PASSWORD=secret
      - DB_DATABASE=community_platform
      - DB_PORT=5432
      - KAFKA_BOOTSTRAP_SERVERS=kafka_dev:9092
      - ENABLE_KAFKA=true
      - PRODUCT_READ_GRPC_URL=product-read:9091 # Correct gRPC URL for product-read
      - INVENTORY_READ_GRPC_URL=inventory-read:50052 # Correct gRPC URL for inventory-read
      - DISCOUNT_ENGINE_URL=http://discount-engine:8085 # Correct HTTP URL for discount-engine
      - TAX_CALCULATION_URL=http://tax-calculation:8087 # Correct HTTP URL for tax-calculation

  auth-service:
    build:
      context: ../../ # Monorepo root
      dockerfile: services/users/auth-service/Dockerfile
      network: host
      extra_hosts:
        - "repo.maven.apache.org:104.18.19.12"
        - "repo1.maven.org:104.18.19.12"
        - "registry.npmjs.org:104.16.2.34"
    container_name: auth_service_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    depends_on:
      - postgres
    ports:
      - "3002:3002"
    environment:
      - PORT=3002
      - DB_HOST=postgres_dev
      - DB_USER=admin
      - DB_PASSWORD=secret
      - DB_DATABASE=community_platform
      - DB_PORT=5432
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres_dev:5432/community_platform
      # Add JWT secrets and other auth-specific config here
      - JWT_SECRET=RjhFY0JDenlOV3NEaVZiR2hIMFVkQk1Qa01oc1JCSFA=
      - JWT_EXPIRES_IN=1h
      - GRPC_SERVER_PORT=6565
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318

  checkout-orchestrator:
    build:
      context: ../../ # Monorepo root
      dockerfile: services/checkout/checkout-orchestrator/Dockerfile
      network: host
      extra_hosts:
        - "repo.maven.apache.org:104.18.19.12"
        - "repo1.maven.org:104.18.19.12"
        - "registry.npmjs.org:104.16.2.34"
    container_name: checkout_orchestrator_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    depends_on:
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
      schema-registry:
        condition: service_started
    ports:
      - "8000:8000" # Maps to 8000 on host
    environment:
      - DATABASE_URL=postgresql://admin:secret@postgres_dev:5432/community_platform
      - KAFKA_BOOTSTRAP_SERVERS=kafka_dev:9092
      - PORT=8000 # Internal port for the FastAPI app
      - DISCOUNT_ENGINE_SERVICE_URL=http://discount-engine:8085
      - TAX_CALCULATION_SERVICE_URL=http://tax-calculation:8087

  gateway-bff:
    build:
      context: ../../ # Monorepo root
      dockerfile: apps/gateway-bff/Dockerfile # Specify the Dockerfile path
      network: host
      extra_hosts:
        - "repo.maven.apache.org:104.18.19.12"
        - "repo1.maven.org:104.18.19.12"
        - "registry.npmjs.org:104.16.2.34"
        - "node-precompiled-binaries.grpc.io:104.21.13.85"
    container_name: gateway_bff_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    depends_on:
      - postgres
      - auth-service
      - product-read
      - cart-crud
    ports:
      - "3004:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
      - NEXT_PUBLIC_AUTH_SERVICE_URL=http://auth-service:3002
      - NEXT_PUBLIC_PRODUCT_READ_SERVICE_URL=http://product-read:8082 # Corrected port
      - NEXT_PUBLIC_CART_CRUD_SERVICE_URL=http://cart-crud:3000 # Corrected internal port
      - NEXT_PUBLIC_INTENT_PARSER_URL=http://intent-parser:8080 # URL for intent-parser
      - AUTH_SERVICE_GRPC_URL=auth-service:6565 # Correct gRPC URL
      - PRODUCT_READ_GRPC_URL=product-read:9091 # Correct gRPC URL
      - INVENTORY_GRPC_URL=inventory-read:50052
      - CART_GRPC_URL=cart-crud:50050
      - NEXT_PUBLIC_CHECKOUT_ORCHESTRATOR_URL=http://checkout-orchestrator:8000
      - NEXT_PUBLIC_DISCOUNT_ENGINE_URL=http://discount-engine:8085
      - NEXT_PUBLIC_TAX_CALCULATION_URL=http://tax-calculation:8087 # Correct gRPC URL
      - PRODUCT_WRITE_SERVICE_URL=http://product-write:8081
      - AUTH_SERVICE_URL=http://auth-service:3002
      - ORDER_CREATE_SERVICE_URL=http://order-create:8094
      - ORDER_READ_SERVICE_URL=http://order-read:8095
      - PAYMENT_GATEWAY_SERVICE_URL=http://payment-gateway:8097
      - CHECKOUT_ORCHESTRATOR_URL=http://checkout-orchestrator:8000/api
      - SEARCH_SERVICE_GRPC_URL=search_query_dev:50053
      - NEXT_PUBLIC_SEARCH_API_URL=/api/search

  checkout-ui:
    build:
      context: ../../ # Monorepo root
      dockerfile: checkout-ui/Dockerfile
      network: host
      extra_hosts:
        - "node-precompiled-binaries.grpc.io:104.21.13.85"
        - "registry.npmjs.org:104.16.2.34"
    container_name: checkout_ui_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4
    depends_on:
      - gateway-bff
    ports:
      - "3005:3000"
    environment:
      - NODE_ENV=development
      - PORT=3000
      - NEXT_PUBLIC_API_BASE_URL=http://localhost:3004 # Host address for client-side calls
      - INTERNAL_API_BASE_URL=http://gateway_bff_dev:3000 # Internal Docker address for server-side calls
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/"]
      interval: 30s
      timeout: 10s
      retries: 3


  cart-snapshot:
    build:
      context: ../../ # Monorepo root
      dockerfile: services/cart/cart-snapshot/Dockerfile # Specify the Dockerfile path
      network: host
      extra_hosts:
        - "repo.maven.apache.org:104.18.19.12"
        - "repo1.maven.org:104.18.19.12"
        - "registry.npmjs.org:104.16.2.34"
    container_name: cart_snapshot_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    depends_on:
      - postgres
      - cart-crud
    ports:
      - "8083:8083"
    environment:
      - SERVER_PORT=8083
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres_dev:5432/community_platform
      - SPRING_DATASOURCE_USERNAME=admin
      - SPRING_DATASOURCE_PASSWORD=secret
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
      # This is the crucial URL for the adapter to find the cart-crud service
      - APP_CLIENTS_CART-CRUD_URL=http://cart_crud_dev:3000

  cart-pricing:
    build:
      context: ../../ # Monorepo root
      dockerfile: services/cart/cart-pricing/Dockerfile
      network: host
      extra_hosts:
        - "repo.maven.apache.org:104.18.19.12"
        - "repo1.maven.org:104.18.19.12"
        - "registry.npmjs.org:104.16.2.34"
    container_name: cart_pricing_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    depends_on:
      - postgres
      - cart-snapshot # Depends on cart-snapshot to get immutable cart data
    ports:
      - "8084:8084"
    environment:
      - SERVER_PORT=8084
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres_dev:5432/community_platform
      - SPRING_DATASOURCE_USERNAME=admin
      - SPRING_DATASOURCE_PASSWORD=secret
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
      # URL for the cart-snapshot service
      - APP_CLIENTS_CART-SNAPSHOT_URL=http://cart_snapshot_dev:8083

  postgres:
    image: postgres:15
    container_name: postgres_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=secret
      - POSTGRES_DB=community_platform
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./../../services/inventory/inventory-write/init.sql:/docker-entrypoint-initdb.d/init_inventory_items.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7
    container_name: redis_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: zookeeper_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"

  kafka:
    image: confluentinc/cp-kafka:7.5.0
    container_name: kafka_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
      - "29092:29092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper_dev:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka_dev:9092,PLAINTEXT_HOST://localhost:29092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 9092 || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 10

  schema-registry:
    image: confluentinc/cp-schema-registry:7.5.0
    container_name: schema_registry_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    depends_on:
      - kafka
    ports:
      - "8086:8085" # Schema Registry UI/API - Mapped to 8086 to avoid conflict with discount-engine
    environment:
      SCHEMA_REGISTRY_HOST_NAME: schema-registry
      SCHEMA_REGISTRY_KAFKASTORE_BOOTSTRAP_SERVERS: kafka_dev:9092
      SCHEMA_REGISTRY_LISTENERS: http://0.0.0.0:8085

  discount-engine:
    build:
      context: ../../ # Monorepo root
      dockerfile: services/pricing/discount-engine/Dockerfile
      network: host
      extra_hosts:
        - "repo.maven.apache.org:104.18.19.12"
        - "repo1.maven.org:104.18.19.12"
        - "registry.npmjs.org:104.16.2.34"
    container_name: discount_engine_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    depends_on:
      - postgres
    ports:
      - "8085:8085"
    environment:
      - SERVER_PORT=8085
      - SPRING_DATASOURCE_URL=jdbc:h2:mem:discountenginedb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE # Using H2 in-memory
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update

  tax-calculation:
    build:
      context: ../../ # Monorepo root
      dockerfile: services/pricing/tax-calculation/Dockerfile
      network: host
      extra_hosts:
        - "repo.maven.apache.org:104.18.19.12"
        - "repo1.maven.org:104.18.19.12"
        - "registry.npmjs.org:104.16.2.34"
    container_name: tax_calculation_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    depends_on:
      - postgres
    ports:
      - "8087:8086"
    environment:
      - SERVER_PORT=8086
      - SPRING_DATASOURCE_URL=jdbc:h2:mem:taxcalcdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE # Using H2 in-memory
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update

  product-lookup-rust:
    build:
      context: ../../ # Monorepo root
      dockerfile: services/catalog/product-lookup-rust/Dockerfile
    container_name: product_lookup_rust_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    depends_on:
      - postgres
    ports:
      - "50051:50051"
    environment:
      - DATABASE_URL=postgres://admin:secret@postgres_dev:5432/community_platform
    extra_hosts:
      - "host.docker.internal:host-gateway"

  inventory-read:
    build:
      context: ../../
      dockerfile: services/inventory/inventory-read/Dockerfile
    container_name: inventory_read_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4
    depends_on:
      - postgres
    ports:
      - "50052:50052"
    environment:
      - DATABASE_URL=postgres://admin:secret@postgres_dev:5432/community_platform
      - GRPC_SERVER_ADDRESS=0.0.0.0:50052
    extra_hosts:
      - "host.docker.internal:host-gateway"

  kafdrop:
    image: obsidiandynamics/kafdrop:latest
    container_name: kafdrop_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    depends_on:
      - kafka
    ports:
      - "9000:9000"
    environment:
      KAFKA_BROKERCONNECT: kafka_dev:9092

  prometheus:
    image: prom/prometheus:v2.47.1
    container_name: prometheus_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    volumes:
      - ./prometheus:/etc/prometheus
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana:10.1.5
    container_name: grafana_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    depends_on:
      - prometheus
    ports:
      - "3003:3000"
    volumes:
      - grafana_data:/var/lib/grafana

  jaeger:
    image: jaegertracing/all-in-one:1.50
    container_name: jaeger_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    ports:
      - "6831:6831/udp"
      - "16686:16686"
      - "4317:4317"
      - "4318:4318"
    command:
      - "--collector.otlp.enabled=true"

  minio:
    image: minio/minio:latest
    container_name: minio_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4

    ports:
      - "9001:9001" # Minio UI
      - "9005:9002" # Minio API (Mapped to 9005 on host)
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data

  meilisearch:
    image: getmeili/meilisearch:v1.5
    container_name: meilisearch_dev
    dns:
      - 8.8.8.8
      - 8.8.4.4
    ports:
      - "7700:7700"
    environment:
      - MEILI_MASTER_KEY=masterKey # In prod, use a strong secret
      - MEILI_ENV=development
    volumes:
      - meili_data:/meili_data

  search-index:
    build:
      context: ../../
      dockerfile: services/search/search-index/Dockerfile
    container_name: search_index_dev
    depends_on:
      - kafka
      - meilisearch
    environment:
      - KAFKA_BOOTSTRAP_SERVERS=kafka_dev:9092
      - PRODUCT_EVENTS_TOPIC=product-events
      - KAFKA_GROUP_ID=search-index-group
      - MEILISEARCH_URL=http://meilisearch_dev:7700
      - MEILISEARCH_API_KEY=masterKey
      - MEILISEARCH_INDEX=products
      - RUST_LOG=info

  search-query:
    build:
      context: ../../
      dockerfile: services/search/search-query/Dockerfile
    container_name: search_query_dev
    depends_on:
      - meilisearch
    ports:
      - "50053:50053"
    environment:
      - MEILISEARCH_URL=http://meilisearch_dev:7700
      - MEILISEARCH_API_KEY=masterKey
      - MEILISEARCH_INDEX=products
      - GRPC_SERVER_PORT=50053
      - RUST_LOG=info

  order-create:
    build:
      context: ../../
      dockerfile: services/orders/order-create/Dockerfile
      network: host
      extra_hosts:
        - "repo.maven.apache.org:104.18.19.12"
        - "repo1.maven.org:104.18.19.12"
        - "registry.npmjs.org:104.16.2.34"
    container_name: order_create_dev
    depends_on:
      - kafka
      - postgres
    ports:
      - "8094:8094"
    environment:
      - SERVER_PORT=8094
      - SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka_dev:9092
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres_dev:5432/community_platform
      - SPRING_DATASOURCE_USERNAME=admin
      - SPRING_DATASOURCE_PASSWORD=secret
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update

  order-read:
    build:
      context: ../../
      dockerfile: services/orders/order-read/Dockerfile
      network: host
      extra_hosts:
        - "repo.maven.apache.org:104.18.19.12"
        - "repo1.maven.org:104.18.19.12"
        - "registry.npmjs.org:104.16.2.34"
    container_name: order_read_dev
    depends_on:
      - postgres
    ports:
      - "8095:8095"
    environment:
      - SERVER_PORT=8095
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres_dev:5432/community_platform
      - SPRING_DATASOURCE_USERNAME=admin
      - SPRING_DATASOURCE_PASSWORD=secret
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update

  payment-gateway:
    build:
      context: ../../
      dockerfile: services/orders/payment-gateway/Dockerfile
      network: host
      extra_hosts:
        - "repo.maven.apache.org:104.18.19.12"
        - "repo1.maven.org:104.18.19.12"
        - "registry.npmjs.org:104.16.2.34"
    container_name: payment_gateway_dev
    depends_on:
      - kafka
      - postgres
    ports:
      - "8097:8097"
    environment:
      - SERVER_PORT=8097
      - SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka_dev:9092
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres_dev:5432/community_platform
      - SPRING_DATASOURCE_USERNAME=admin
      - SPRING_DATASOURCE_PASSWORD=secret
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update

volumes:
  postgres_data:
  redis_data:
  prometheus_data:
  grafana_data:
  minio_data:
  meili_data:
