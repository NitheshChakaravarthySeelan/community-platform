apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-sms-service
  labels:
    app: sms-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: sms-service
  template:
    metadata:
      labels:
        app: sms-service
    spec:
      containers:
        - name: sms-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          resources: {}
          env: []
