apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-order-read
  labels:
    app: order-read
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: order-read
  template:
    metadata:
      labels:
        app: order-read
    spec:
      containers:
        - name: order-read
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          resources: {}
          env: []
