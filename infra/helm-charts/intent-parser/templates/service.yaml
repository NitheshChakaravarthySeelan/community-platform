apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-intent-parser
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: intent-parser
