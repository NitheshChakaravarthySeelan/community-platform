apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-product-read
  labels:
    app: product-read
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: product-read
  template:
    metadata:
      labels:
        app: product-read
    spec:
      containers:
        - name: product-read
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          resources: {}
          env: []
