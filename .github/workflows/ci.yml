name: CI

on: [push, pull_request]

jobs:
build:
 runs-on: ubuntu-latest
 steps:
   - uses: actions/checkout@v4
   - name: Set up JDK 17
     uses: actions/setup-java@v4
     with:
       distribution: temurin
       java-version: 17
   - name: Set up Node
     uses: actions/setup-node@v4
     with:
       node-version: 18
   - name: Build Java modules
     run: |
       echo "Skip: add maven build steps per-service"
   - name: Lint JS
     run: |
       echo "Skip: add frontend lint steps"
